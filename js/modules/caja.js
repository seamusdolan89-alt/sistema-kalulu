/**
 * caja.js — Cash Management Module
 */

export default {
  init(params) {
    console.log('💰 Caja module initialized', params);
    // TODO: Cash session management, reconciliation, registers
  }
};
